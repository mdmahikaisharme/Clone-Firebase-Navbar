import type { NextPage } from "next";
import { Header, Main } from "@component";

export default function admin({}: NextPage) {
    return (
        <>
            <div className="">ADMIN PANEL</div>
        </>
    );
}
